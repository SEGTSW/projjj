# AI Testing Strategy

This file mirrors `testing.md` for agents that search for explicit coverage guidance.

Quality targets:

- 70%+ coverage gate in Sonar.
- 200+ unit and integration tests.
- 0 known bugs and vulnerabilities.
- Reports generated on every CI run and uploaded as artifacts.

Do not introduce a test framework that requires a service account or external database.
